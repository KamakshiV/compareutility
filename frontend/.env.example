# Local dev: leave unset so `npm run dev` uses the Vite `/api` proxy (no CORS setup).
# VITE_API_BASE=http://127.0.0.1:8000
#
# Vercel / production: set to your public API origin, e.g. https://api.example.com
# VITE_API_BASE=
